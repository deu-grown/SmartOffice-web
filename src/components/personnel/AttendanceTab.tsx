// 인사 페이지 내 근태 탭 — C7(features/attendance) 진입 시 ADMIN 근태 3종(목록·수동보정·배치 트리거)과 연결될 placeholder.
// C0 빈 분할 단계에서는 사용처가 없으며, 마스터플랜 G3 분할 가이드의 5 파일 구성을 만족시키기 위한 골격.

export function AttendanceTab() {
  return null;
}
