// 주차면(ParkingSpot) CRUD 표 — 묶음 3 분할 시점 placeholder.
// 묶음 4 흡수 시점에 useParkingSpots(filter) + filter UI(ZoneSelect + spotType + status) + 등록/수정/삭제 모달 활성화.
export function ParkingSpotsTable() {
  return (
    <div className="bg-white p-10 rounded-[50px] border border-gray-100 shadow-sm text-center text-sm text-gray-400">
      주차면 관리 표는 묶음 4 에서 활성화됩니다.
    </div>
  );
}
